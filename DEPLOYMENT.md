# Инструкция по развертыванию "Зимняя лаборатория чудес"

## Подготовка к деплою

### 1. Проверка файлов

Убедитесь, что все необходимые файлы на месте:
- ✅ `index.html` - главная страница
- ✅ `css/style.css` - стили
- ✅ `js/main.js` - основная логика
- ✅ `js/predictions.js` - данные предсказаний и мероприятий
- ✅ `assets/images/` - все изображения
- ✅ Favicon файлы в `assets/images/`

### 2. Настройка ссылок

Проверьте и обновите ссылки в `js/main.js`:
- Telegram бот (строка ~108)
- VKPlay ссылка (строка ~113)

### 3. Проверка изображений

Убедитесь, что все изображения загружены:
- `assets/images/background/lab-background-far.png`
- `assets/images/background/lab-background.png`
- `assets/images/interactive/monitor-screen.png`
- `assets/images/interactive/map-tablet.png`
- `assets/images/event-*.png` (7 файлов)
- Favicon файлы

## Размещение на GitHub

### Шаг 1: Инициализация репозитория

```bash
# Если репозиторий еще не инициализирован
git init
git add .
git commit -m "Initial commit: Зимняя лаборатория чудес"
```

### Шаг 2: Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Создайте новый репозиторий (например, `winter-lab-wonders`)
3. Не добавляйте README, .gitignore или лицензию (они уже есть)

### Шаг 3: Подключение к GitHub

```bash
git remote add origin https://github.com/ваш-username/winter-lab-wonders.git
git branch -M main
git push -u origin main
```

## Размещение на сервере

### Вариант 1: Автодеплой через GitHub (рекомендуется)

#### Timeweb Cloud / Beget / Другие хостинги с Git

1. Войдите в панель управления хостингом
2. Найдите раздел "Git" или "Автодеплой"
3. Подключите ваш GitHub репозиторий
4. Настройте:
   - **Ветка**: `main`
   - **Команда сборки**: (не требуется, статический сайт)
   - **Папка публикации**: `/` или `public_html`
   - **Автодеплой**: включить

### Вариант 2: Ручная загрузка через FTP/SFTP

1. Подключитесь к серверу через FTP-клиент (FileZilla, WinSCP)
2. Загрузите все файлы в корневую директорию сайта:
   - Обычно это `public_html` или `www`
3. Сохраните структуру папок:
   ```
   public_html/
   ├── index.html
   ├── css/
   ├── js/
   ├── assets/
   └── ...
   ```

### Вариант 3: Через SSH

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Перейдите в директорию сайта
cd /var/www/html  # или ваша директория

# Клонируйте репозиторий
git clone https://github.com/ваш-username/winter-lab-wonders.git .

# Или обновите существующий
git pull origin main
```

## Настройка веб-сервера

### Apache (.htaccess)

Создайте файл `.htaccess` в корне проекта:

```apache
# Включить перезапись URL
RewriteEngine On

# Перенаправление на index.html для всех запросов
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.html [L,QSA]

# Кэширование статических файлов
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# Сжатие файлов
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>
```

### Nginx

Добавьте в конфигурацию сайта:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статических файлов
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## Проверка после деплоя

1. ✅ Откройте сайт в браузере
2. ✅ Проверьте все интерактивные элементы:
   - Планшет с картой
   - Карточки мероприятий
   - Монитор с игрой
   - Генератор предсказаний
3. ✅ Проверьте загрузку изображений
4. ✅ Проверьте работу на мобильных устройствах
5. ✅ Проверьте favicon в браузере

## Оптимизация производительности

### Минификация (опционально)

Для продакшена можно минифицировать CSS и JS:

```bash
# Установите инструменты (если нужно)
npm install -g clean-css-cli uglify-js

# Минификация CSS
cleancss -o css/style.min.css css/style.css

# Минификация JS
uglifyjs js/main.js js/predictions.js -o js/bundle.min.js
```

### Оптимизация изображений

- Используйте WebP формат для современных браузеров
- Сжимайте PNG/JPG перед загрузкой
- Используйте инструменты: TinyPNG, ImageOptim

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь, что все пути к файлам корректны
4. Проверьте права доступа к файлам на сервере

---

**Успешного деплоя!** 🚀✨

